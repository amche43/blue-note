# 0.3 本机社区验证记录

日期：2026-09-09。

- Flutter 17 项测试通过，包含反馈离线草稿、关闭重开、同编号重试、账户绑定、320px 窄屏反馈表单、禁用未连接提交按钮，以及真实 Dart 客户端对本机 HTTPS 的证书校验和账户验证。既有学习、笔记、自建题库测试同时通过。
- Python 后台端到端测试通过：无凭据拒绝、必须同意分享、去除无关私人字段、双账户权限、点赞幂等、评论幂等与作者删除、反馈回执、同编号不同内容拒绝、反馈仅本人可见、管理状态更新、数据库重新打开保留数据、撤回后停止下载。
- 静态分析无问题。使用已有依赖运行，没有因在线依赖审计连接失败而跳过代码检查。
- 实际启动脚本生成了本机证书与两个测试身份，后台成功监听 127.0.0.1:8788。
- APK release 构建成功；版本 0.3.0 / versionCode 3，应用包名 cn.bluepen.learn.blue_note，Android 7.0 起，3 个 CPU 架构。
- APK v2 签名有效，0.2 与 0.3 签名证书一致，可覆盖安装。
- APK 57,432,714 字节；SHA-256：`e7c3a056be4a029ccbd12d8cfed005c9a151b41d58df6032317f154cc678218b`。

限制：助手未完成 Android 模拟器里的 0.3 端到端操作。真实 HTTPS 测试使用桌面 Dart 运行时连接同一后台，不能替代模拟器网络映射验收。拍照识题、互联网部署、公开注册、网页管理页面未实现。当前管理反馈使用本机命令窗口。

运行相关日志位于工作目录 `work/tests-03-final.log`、`work/analyze-03-final.log`、`work/android-build-03.log`。真实账户、私钥、社区数据库不打包到源码或 APK。
