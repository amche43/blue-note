# 验证记录 · 2026-09-09

## 已通过

- Flutter 静态检查：无问题。
- 应用自动化测试 13 项全部通过：覆盖原有学习流程，并新增自建题修订与删除、重开保存、v1/v2 备份、分享必须明确同意、分享不含私人笔记、重复导入不覆盖编辑、无解析题目保存和知识点候选。
- 实际 Flutter 首页及录题页渲染预览；检查 390 和 320 逻辑像素宽度。
- Java 服务测试：认证、两份客户端数据合并、重复同步去重、冲突请求整体拒绝、服务重启后数据保留，以及 v1/v2 自建题同步与无效题目拒绝。

## 当前构建状态

0.2 Android release APK 已于 2026-09-09 在当前环境增量构建成功，并完成后续校验。尚未在实体手机或安卓模拟器中运行验证。

- 文件：blue-note-0.2.0.apk，57,087,682 字节（约 54.4 MiB）。
- 包名：cn.bluepen.learn.blue_note；版本：0.2.0（2）。
- 应用名称：蓝笔；最低 Android API 24，目标 API 36。
- APK v2 签名校验通过；当前为个人试用的开发签名。与保留的 0.1.0 APK 签名证书一致，可覆盖更新。
- ZIP 内容完整性通过，包含 7 个单元、16 道变式。
- arm64-v8a、armeabi-v7a、x86_64 均包含应用与 Flutter 原生库。
- SHA-256：6cd74405c541402651894eaa5d98d1f5a368215060a2db331dfc963b75347829。

NDK 28.2.13676358 已按官方校验值核对；SDK 许可已完成。本机工作缓存使用阿里云公共 HTTPS 镜像访问 Maven Central 依赖。源工程保留标准仓库配置。

当前自动执行环境的 Java 真实路径解析曾返回 AccessDeniedException；用户通过 build-android.cmd 在本机终端完成构建。没有修改系统 ACL 或关闭安全检查。启动脚本已修正 PowerShell 将下载源提示显示为 NativeCommandError 的问题，并验证仍能保留真实失败的退出码。

## 尚未验证或完成

- 手机通过有效 HTTPS 入口完成实际跨设备同步；当前未部署云服务。
- iOS 编译与真机安装；当前只有共用代码和平台工程。
- 自动读取 PDF、拍照识题和自动生成内容；当前通过对话人工核对后更新内容包。
- 全科题库；当前覆盖高数、数据结构、计算机网络，合计 7 个单元、16 道变式。

预览图片来自应用的 Flutter 测试渲染，并非安卓真机截图。通过自动化测试不等于完成实体手机验收。
