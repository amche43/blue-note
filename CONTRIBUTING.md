# 一起完善蓝笔

欢迎提交功能改进、错误报告和原创题目。功能代码与题库内容分开评审。

## 提交代码

先提交一个具体问题或改进目标。修复应附相关验证，涉及照片、登录、备份或数据迁移时说明数据流与失败处理。不要提交真实口令、签名私钥、用户数据、模型缓存或PDF扫描件。

Flutter：`flutter analyze` 和 `flutter test`。本机HTTPS集成测试需要先启动测试后台并明确传入 `--dart-define=BLUE_NOTE_LOCAL_TEST=true`。

社区后端：`python server/test_community.py`（当前脚本的OpenSSL路径需按开发电脑调整）。OCR依赖是可选的，见 `server/requirements-ocr.txt`。普通学习和题库编辑无需识别模型。

## 提交题目

请提供题干完整条件、来源或原创说明、独立题解、触发条件、关键动作、适用边界，以及至少一道经过核对的变式。可参考 `docs/内容整理规范.md`。

错题本名不是唯一标识。同名本子按作者和内部编号区分；题号固定，删除不会重新排列。讨论和纠错对应具体公开版本。

用户照片识别结果必须经过核对，不能将OCR输出直接标为已审核题目。不上传个人笔记或整本出版物扫描件到公共仓库。代码开源不等于对所有第三方题目授予再分发许可。

发现题解问题，可使用App纠错反馈或GitHub issue，注明版本、题目来源和可复现的推导。
