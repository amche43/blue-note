name: blue_note
description: 从例题出发，记住下一次解题的关键一步。
publish_to: none
version: 0.26.1+27
environment:
  sdk: '>=3.10.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  sqflite: ^2.4.2
  path: ^1.9.1
  flutter_math_fork: ^0.7.4
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  sqflite_common_ffi: ^2.3.6
flutter:
  uses-material-design: true
  assets:
    - assets/lessons.json
    - assets/brand/launcher.png
    - assets/brand/ui-reference.png
